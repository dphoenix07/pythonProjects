class FlightData:
    #This class is responsible for structuring the flight data.
    # def __init__(self):
    #     self.now = datetime.now()
    #     self.to_date = datetime(now.year, now.month+6, now.day)
    #     self.to_location = "JFK"
    #     self.from_location = "SYR"
    #
    # search_body = {
    #     "fly_from": self.from_location,
    #     "fly_to": self.to_location,
    #     "dateFrom": self.now.strftime("%d/%m/%Y"),
    #     "dateTo": self.to_date
    # }
    #
    # response = requests.get(url=SEARCH_ENDPOINT, api)
    pass